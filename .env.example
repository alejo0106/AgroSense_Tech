# Copy this file to .env and adjust values for local development.
# The app reads .env automatically if python-dotenv is installed (it's in requirements.txt).

# Option A: Single DSN
# DATABASE_URL=postgresql://postgres:root@localhost:5433/AgroSense_Tech

# Option B: Individual parts (the app will construct the DSN automatically)
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=root
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5433
# POSTGRES_DB=AgroSense_Tech

# Optionally, you can set an explicit DSN here (overrides the above)
# POSTGRES_DSN=postgresql://postgres:root@localhost:5433/AgroSense_Tech
